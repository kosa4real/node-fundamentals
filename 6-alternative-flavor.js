module.exports.items = ['item1', 'item2']

const person = {
  name: 'Nura'
}

module.exports.singlePerson = person;

//coz when we console:
//console.log(moudle)
//we will see an with exports object as a property inside the module
//hence we can add a new key/value pair